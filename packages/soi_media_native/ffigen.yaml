# Run with `dart run ffigen --config ffigen.yaml`.
name: SoiMediaNativeBindings
description: |
  Bindings for `src/soi_media_native.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/soi_media_native_bindings_generated.dart'
headers:
  entry-points:
    - 'src/soi_media_native.h'
  include-directives:
    - 'src/soi_media_native.h'
ffi-native:
comments:
  style: any
  length: full
