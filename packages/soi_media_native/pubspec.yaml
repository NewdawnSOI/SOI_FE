name: soi_media_native
description: "Native image and waveform helpers for SOI."
version: 0.0.1

environment:
  sdk: ^3.8.0

dependencies:
  ffi: ^2.1.4
  flutter:
    sdk: flutter
  code_assets: ^1.0.0
  hooks: ^1.0.0
  native_toolchain_c: ^0.17.4

dev_dependencies:
  ffigen: ^20.1.1
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  test: ^1.28.0
