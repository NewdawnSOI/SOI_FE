name: tagging_flutter
description: Flutter widgets for anchored tagging flows.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  cached_network_image: ^3.4.1
  flutter_screenutil: ^5.9.0
  shimmer: ^3.0.0
  tagging_core:
    path: ../tagging_core

dev_dependencies:
  flutter_test:
    sdk: flutter
